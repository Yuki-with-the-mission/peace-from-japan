export default function BackgroundPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">Background</h1>
      <p className="mt-2 text-gray-600">歴史・当事者・国際法の要点（ドラフト v0.1）。</p>
      <p className="mt-4 text-sm text-gray-500">※ まずはプレースホルダー。本文は /data/background/ 以下に追加予定。</p>
    </main>
  );
}
